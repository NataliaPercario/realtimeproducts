<h1> Elegí tus zapas </h1>

<div class="contenedorCards">
  {{#each products}}
    <div class="card">
      <p> {{this.title}} </p>
      <p> {{this.description}} </p>
      <p> {{this.price}} </p>
    </div>
  {{/each}}
</div>